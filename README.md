# 🚀 دليل إعداد منصة CryptoMine Pro

هذا الدليل سيساعدك على ربط المنصة بقاعدة بيانات (Google Sheets) وتفعيلها على GitHub.

## 1️⃣ إعداد Google Sheets
1. قم بإنشاء ملف Google Sheets جديد.
2. قم بتسمية الملف بـ `CryptoMine_DB`.
3. أنشئ التبويبات (Sheets) التالية بأسماء دقيقة وضع العناوين في الصف الأول:
   - **Users**: `id`, `username`, `email`, `password`, `role`, `balance_usdt`, `created_at`
   - **MiningPlans**: `id`, `name`, `price`, `hash_power`, `duration_days`, `daily_return`
   - **Transactions**: `id`, `user_id`, `type`, `amount`, `currency`, `address`, `status`, `tx_hash`, `created_at`
   - **UserContracts**: `id`, `user_id`, `plan_id`, `start_date`, `end_date`, `earned_amount`, `status`

## 2️⃣ إعداد وتدشين البرمجة (Google Apps Script)
1. في ملف Google Sheets، اذهب إلى `Extensions` > `Apps Script`.
2. انسخ محتوى الملف `bridge_script.js` المرفق في الكود والصقه في محرّر سكريبت جوجل.
3. **مهم جداً:** في محرر الأكواد، اختر وظيفة `initDatabase` من القائمة المنسدلة في الأعلى واضغط على زر **Run**. هذا سيقوم بإنشاء جميع الجداول والتنسيقات تلقائياً في ملف الإكسل الخاص بك.
4. اضغط على `Deploy` > `New Deployment`.
5. اختر `Web App`.
6. في اختيار `Execute as` اختر **Me**.
7. في اختيار `Who has access` اختر **Anyone**.
8. اضغط `Deploy` وانسخ الرابط (Web App URL).

## 3️⃣ ربط الواجهة الأمامية (Frontend)
1. افتح ملف `api_bridge.js`.
2. استبدل `YOUR_DEPLOYED_GAS_WEB_APP_URL` بالرابط الذي نسخته في الخطوة السابقة.

## 4️⃣ النشر على GitHub
1. ارفع جميع الملفات إلى مستودع (Repository) جديد على GitHub.
2. اذهب إلى `Settings` > `Pages`.
3. اختر `Deploy from a branch` واجعلها `main` أو `master`.
4. مبروك! موقعك الآن متاح للجميع.

---
**⚠️ ملاحظة أمنية:** هذا الهيكل يستخدم Google Sheets كقاعدة بيانات مبسطة. للمشاريع الضخمة التي تضم آلاف المستخدمين، يفضل استخدام PostgreSQL أو MongoDB.
